after every move is completed the game state is saved to disk.

during a move this allows the game model to be modified freely, because in the case of an error it can easily be reverted to the last saved state.

GET MARBLES 1
select row or col to shift
get equivalent resources
the user selects those to discard
    if they're not enough, the server sends an error, user has to try again
    if they're ok, the server rearranges automatically the shelves to fit all of them
    (the user can always switch them shelves to his liking)